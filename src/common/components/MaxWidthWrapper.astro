---
import { cn } from "@lib/utils";

interface WrapperProps {
  className?: string;
}

const { className }: WrapperProps = Astro.props;
---

<div class={cn("mx-auto w-full max-w-screen-2xl px-4 md:px-20", className)}>
  <slot />
</div>
